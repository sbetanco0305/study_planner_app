import WorkstreamPlaceholder from "../components/WorkstreamPlaceholder";

export default function Profile() {
  return (
    <WorkstreamPlaceholder
      title="Profile"
      owner="Shared Navigation"
      description="Profile editing and related settings were removed so the remaining code reflects the project shell plus the dashboard-only implementation."
      badge="Route Scaffold"
    />
  );
}
