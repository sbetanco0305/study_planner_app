import WorkstreamPlaceholder from "../components/WorkstreamPlaceholder";

export default function Assignments() {
  return (
    <WorkstreamPlaceholder
      title="Assignments"
      owner="Person 3"
      description="This route is still wired into the app so the shared navigation works, but the assignments list, details, and add or edit logic were intentionally removed."
    />
  );
}
