import WorkstreamPlaceholder from "../components/WorkstreamPlaceholder";

export default function Calendar() {
  return (
    <WorkstreamPlaceholder
      title="Calendar"
      owner="Person 4"
      description="The calendar page remains part of the routed shell only. Weekly and monthly planning views plus deadline displays are not included in the Person 1 and 2 version."
    />
  );
}
